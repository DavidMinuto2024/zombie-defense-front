/**
 * Production environment. apiKey should be set via build replacement or CI.
 */
export const environment = {
  production: true,
  apiBaseUrl: 'https://zombie-defense-ag.onrender.com',
  apiKey: 'X-API-KEY-ZombieDefense206!',
};
